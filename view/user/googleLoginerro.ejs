<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login Error</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            background: linear-gradient(135deg, #f5f7fa 0%, #e4e9f2 100%);
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, sans-serif;
            padding: 20px;
        }

        .error-container {
            background: white;
            padding: 2.5rem;
            border-radius: 16px;
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.08);
            max-width: 480px;
            width: 100%;
            text-align: center;
            animation: slideUp 0.6s cubic-bezier(0.68, -0.55, 0.265, 1.55);
        }

        @keyframes slideUp {
            0% {
                transform: translateY(40px);
                opacity: 0;
            }
            100% {
                transform: translateY(0);
                opacity: 1;
            }
        }

        .error-icon {
            width: 90px;
            height: 90px;
            margin: 0 auto 1.5rem;
            position: relative;
            animation: shake 0.82s cubic-bezier(.36,.07,.19,.97) both;
        }

        @keyframes shake {
            10%, 90% { transform: translate3d(-1px, 0, 0); }
            20%, 80% { transform: translate3d(2px, 0, 0); }
            30%, 50%, 70% { transform: translate3d(-4px, 0, 0); }
            40%, 60% { transform: translate3d(4px, 0, 0); }
        }

        .error-circle {
            position: absolute;
            width: 100%;
            height: 100%;
            border-radius: 50%;
            background: #ff626226;
            animation: pulse 2s infinite;
        }

        .error-icon::after {
            content: '!';
            position: absolute;
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%);
            font-size: 3rem;
            font-weight: bold;
            color: #ff6262;
        }

        @keyframes pulse {
            0% { transform: scale(0.95); opacity: 0.5; }
            50% { transform: scale(1); opacity: 1; }
            100% { transform: scale(0.95); opacity: 0.5; }
        }

        .error-title {
            color: #2d3748;
            font-size: 1.8rem;
            margin-bottom: 1rem;
            font-weight: 600;
        }

        .error-message {
            color: #718096;
            margin-bottom: 2rem;
            line-height: 1.6;
        }

        .error-code {
            font-family: monospace;
            background: #f7fafc;
            padding: 0.5rem 1rem;
            border-radius: 4px;
            color: #718096;
            font-size: 0.9rem;
            margin-bottom: 2rem;
            display: inline-block;
        }

        .button-group {
            display: flex;
            gap: 1rem;
            justify-content: center;
        }

        .button {
            padding: 0.8rem 1.5rem;
            border-radius: 8px;
            font-weight: 500;
            transition: all 0.2s ease;
            text-decoration: none;
            cursor: pointer;
        }

        .primary-button {
            background: #4a90e2;
            color: white;
            border: none;
        }

        .primary-button:hover {
            background: #357abd;
            transform: translateY(-2px);
        }

        .secondary-button {
            background: transparent;
            color: #4a90e2;
            border: 2px solid #4a90e2;
        }

        .secondary-button:hover {
            background: #4a90e21a;
            transform: translateY(-2px);
        }

        .timestamp {
            margin-top: 2rem;
            color: #a0aec0;
            font-size: 0.85rem;
        }
    </style>
</head>
<body>
    <div class="error-container">
        <div class="error-icon">
            <div class="error-circle"></div>
        </div>
        <h1 class="error-title">Login Error</h1>
        <p class="error-message">
            "We encountered an error while processing your login request. Please try again." 
        </p>
        
        <div class="button-group">
            <a href="/user/login" class="button primary-button">Try Again</a>
            <a href="https://support.google.com/accounts/troubleshooter/2402620?hl=en/" class="button secondary-button">Get Help</a>
        </div>
    </div>
</body>
</html>