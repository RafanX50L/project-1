<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Product is unavailable</title>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/feather-icons/4.29.0/feather.min.js"></script>
    <script src="https://unpkg.com/feather-icons"></script>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, sans-serif;
        }

        body {
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            background-color: #f3f4f6;
        }

        .container {
            text-align: center;
            padding: 2rem;
            max-width: 28rem;
        }

        .icon-wrapper {
            background-color: #fee2e2;
            width: 80px;
            height: 80px;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            margin: 0 auto 2rem;
        }

        .icon-wrapper i {
            color: #ef4444;
        }

        h1 {
            color: #111827;
            font-size: 1.875rem;
            font-weight: 700;
            margin-bottom: 1rem;
        }

        p {
            color: #4b5563;
            line-height: 1.5;
            margin-bottom: 2rem;
        }

        .home-button {
            display: inline-flex;
            align-items: center;
            gap: 0.5rem;
            background-color: #2563eb;
            color: white;
            padding: 0.75rem 1.5rem;
            border-radius: 0.5rem;
            text-decoration: none;
            font-weight: 500;
            transition: background-color 0.2s;
            border: none;
            cursor: pointer;
        }

        .home-button:hover {
            background-color: #1d4ed8;
        }

        @keyframes fadeIn {
            from {
                opacity: 0;
                transform: translateY(20px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        .container {
            animation: fadeIn 0.5s ease-out;
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="icon-wrapper">
            <!-- <i data-feather="package-x" size="32"></i> -->
             <img style="width: 70%;" src="/package-x.915x1024.png" alt="tretrew">
        </div>
        
        <h1>Product Unavailable</h1>
        <p>We're sorry, but the product you're looking for is currently not available. Please check back later or explore our other products.</p>
        
        <a href="/" class="home-button">
            <i data-feather="home" size="18"></i>
            Back to Home
        </a>
    </div>

    <script>
        feather.replace();
    </script>
</body>
</html>